{
  "name": "useful-middy-example",
  "version": "1.0.0",
  "description": "Example of how to use useful middy",
  "main": "handler.js",
  "scripts": {
    "deploy": "npm run clean && npm run setup && sls deploy",
    "setup": "git clone https://github.com/anglinb/useful-middy",
    "clean": "rm -rf useful-middy",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "middy": "^0.13.1",
    "content-type": "^1.0.4",
    "debug": "^3.1.0",
    "http-errors": "^1.6.3"
  }
}
